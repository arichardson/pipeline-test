@Library('ctsrd-jenkins-scripts') _

addJobProperties([
		[$class: 'CopyArtifactPermissionProperty', projectNames: '*'],
		durabilityHint('PERFORMANCE_OPTIMIZED'),
])
echo("${getJobProperties()}")
addJobProperties([
		[$class: 'CopyArtifactPermissionProperty', projectNames: '*'],
		durabilityHint('PERFORMANCE_OPTIMIZED'),
])
echo("${getJobProperties()}")
